# Frontend Environment Variables Example
# Local development uses the Vite proxy. For a separately deployed frontend,
# set this to the backend origin, for example https://api.example.com.
VITE_API_BASE_URL=
# Legacy alias still supported for existing deployments.
VITE_API_URL=
